---

- name: Docker oude versies checken
  package:
    name:
    - docker
    - docker-common
    - docker-engin
    state: absent

- name:  Docker GPG key toevoegen.
  rpm_key:
    key: "{{ docker_yum_gpg_key }}"
    state: present

- name: Docker repository toeveoegen.
  get_url:
    url: "{{ docker_yum_repo_url }}"
    dest: '/etc/yum.repos.d/docker-{{ docker_edition }}.repo'
    owner: root
    group: root
    mode: 0644

- name: Docker installeren.
  package:
    name: "{{ docker_package }}"
    state: "{{ docker_package_state }}"
  notify: restart docker

- name: Verificatie docker enabled en gestart.
  service:
    name: docker
    state: "{{ docker_service_state }}"
    enabled: "{{ docker_service_enabled }}"

